#include <iostream>
using namespace std ;
int main () {
	int t ;
	unsigned long m , n ;
	cin >> t ;
	bool
	for(int i = 0 ; i < t ; i++) {
		cin >> m >> n ;
		if(m % 2 == 0)
			m++ ;
		for(unsigned long j = m ; j <= n ; j = j + 2) {
			
		}
	}
}
